<div class="row">
    {{ form_start(form, {attr: {class: 'filter js-filter-form'}}) }}
        <table class="table table-sm table-striped">
            <thead>
                {% include 'workorder/_filter.html.twig' %}
                {% include 'workorder/_sorting.html.twig' %}
            </thead>
            <tbody class="js-filter-content essai">
                {% include 'workorder/_workorders.html.twig' %}
            </tbody>
        </table>
    {{ form_end(form) }}
</div>