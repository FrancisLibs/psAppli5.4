{% extends 'base.html.twig' %}

{% block title %}Bons de travail{% endblock %}

{% block body %}
    <header class="row mt-5">
        <div class="col-3 offset-2">
            <h2>Bons de travail</h2>
        </div>
    </header>
    <main class="row workorder-js-filter">
        <aside class="col-2 mt-4">
            <a class="btn btn-primary btn-sm mb-3" href="{{ path('work_order_new') }}" role="button">Nouveau bon de travail</a>
            {% include 'workorder/_filter.html.twig' %}
        </aside>
        <article class="col-10">
            <div class="mt-4">
                {% include 'workorder/_workorderList.html.twig' with {workorders: workorders} only %}
            </div>
            {# display navigation #}
            <div class="navigation js-filter-pagination">
                {% include 'workorder/_pagination.html.twig' %}
            </div>
        </article>
    </main>
{% endblock %}