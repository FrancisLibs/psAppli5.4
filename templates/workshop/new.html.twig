{% extends 'base.html.twig' %}

{% block title %}Nouvel atelier{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-8 mt-4 mb-4">
                <div class="row align-items-center mb-4">
                    <div class="col-5">
                        <h2>Création atelier</h2>
                    </div>
                    <div class="col-3">
                        <a class="btn btn-primary btn-sm" href="{{ path('workshop_index') }}" role="button">Liste des ateliers</a>
                    </div>
                </div>
                <div class="row">
            <div class="col-12 mb-4">
                {{ include('workshop/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}