{% extends 'base.html.twig' %}

{% block title %}Atelier - Edition{% endblock %}

{% block body %}
    <div class="row mt-5">
        <div class="col-6 mx-auto">
            <div class="row align-items-center">
                <div class="col-3">
                    <h3>Edition atelier</h3>
                </div>
                <div class="d-flex justify-content-start">
                    <a class="btn btn-primary btn-sm mr-3" href="{{ path('workshop_index') }}" role="button">Liste des ateliers</a>
                    {{ include('workshop/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
    <article class="row">
        <div class="mt-4 col-6 mx-auto">
            {{ include('workshop/_form.html.twig') }}
        </div>
    </article>
{% endblock %}