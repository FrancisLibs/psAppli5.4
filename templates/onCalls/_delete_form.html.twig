<form method="post" action="{{ path('onCall_delete', {'id': onCall.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ onCall.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
