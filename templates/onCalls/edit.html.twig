{% extends 'base.html.twig' %}

{% block title %}Edit Oncall{% endblock %}

{% block body %}
    <div class="row mt-5">
        <div class="col-6 offset-2">
            <div class="row">
                <div class="col-7 d-flex justify-content-between align-items-center">
                    <h3>Edition Rapport d'astreinte</h3>
                    <div class="d-flex justify-content-start">
                        <a class="btn btn-primary btn-sm me-3" href="{{ path('onCall_index') }}" role="button">Retour à la liste</a>
                        {{ include('onCalls/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <article class="row">
        <aside class="col-2">
            {% include 'user/_photo.html.twig' %}
        </aside>
        <div class="mt-4 mb-5 col-9">
            {{ include('onCalls/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
    </article>
{% endblock %}
