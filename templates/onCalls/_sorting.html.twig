<tr>
    <th>{{ knp_pagination_sortable(onCalls, 'Date', 'o.callDay') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(onCalls, 'Heure', 'o.callTime') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(onCalls, 'Problèmes', 'o.why') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(onCalls, 'Travaux', 'o.task') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(onCalls, 'Technicien', 'u.user') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(onCalls, 'Status', 'o.status') }}</th>
</tr>