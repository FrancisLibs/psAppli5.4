{% extends 'base.html.twig' %}

{% block title %}Machine - Edition{% endblock %}

{% block body %}
    <div class="row mt-5">
        <div class="col-6 offset-2">
            <div class="row">
                <div class="col-6 d-flex justify-content-between align-items-center">
                    <h3>Edition machine</h3>
                    <div class="d-flex justify-content-start">
                        <a class="btn btn-primary btn-sm me-3" href="{{ path('machine_index') }}" role="button">Liste des machines</a>
                        {{ include('machine/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <article class="row mt-5">
         <aside class="col-2">
            {% include 'user/_photo.html.twig' %}
        </aside>
        <div class="mt-4 col-8">
            {{ include('machine/_form.html.twig') }}
        </div>
    </article>
{% endblock %}