<form method="post" action="{{ path('part_delete', {'id': part.id}) }}" onsubmit="return confirm('Es-tu sûr de vouloir supprimer cette pièce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ part.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
