{% extends 'base.html.twig' %}

{% block title %}Pièce détachée - Edition{% endblock %}

{% block body %}
    <div class="row mt-5">
        <div class="col-6 offset-2">
            <div class="row">
                <div class="col-6 d-flex justify-content-between align-items-center">
                    <h3>Edition pièce</h3>
                    <div class="d-flex justify-content-start">
                        <a class="btn btn-primary btn-sm me-3" href="{{ path('part_index') }}" role="button">Retour à la liste</a>
                        {{ include('part/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <article class="row">
        <aside class="col-2">
            {% include 'user/_photo.html.twig' %}
        </aside>
        <div class="mt-4 mb-5 col-6">
            {{ include('part/_form.html.twig') }}
        </div>
    </article>
{% endblock %}