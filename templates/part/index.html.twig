{% extends 'base.html.twig' %}

{% block title %}Hello PartController!{% endblock %}

{% block body %}
     <div class="row mt-5">
        <nav class="col-2">
        </nav>
        <section class="col-8">
            <div class="row mb-3">
                <div class="col-4">
                    <h3>Pièces détachées</h3>
                </div>
                <div class="col-3">
                    <a href="#" class="btn btn-primary btn-sm">Ajouter pièce</a>
                </div>
            </div>
            {% include('part/_partsList.html.twig') %}
            {# display navigation #}
            <div class="navigation">
                {{ knp_pagination_render(parts) }}
            </div>
        </section>
        <aside class="col-2">
        </aside>
    </div>
{% endblock %}
