{% extends 'base.html.twig' %}

{% block title %}Gmao PS bienvenue !{% endblock %}

{% block body %}
    <div class="row mt-3">
        <nav class="col-3">
            <a href="#" class="btn btn-primary btn-sm w-50 mb-2">Pièces détachées</a><br>
            <a href="#" class="btn btn-primary btn-sm w-50 mb-2">Machines</a><br>
            <a href="#" class="btn btn-primary btn-sm w-50 mb-2">Nouveau BT</a><br>
            <a href="#" class="btn btn-primary btn-sm w-50">Préventif</a>
        </nav>
        <section class="col-6">
            <div class="row mb-3">
                <div class="col-3">
                    <h3>BT en cours</h3>
                </div>
                <div class="col-2">
                    <a href="#" class="btn btn-primary btn-sm">Nouveau BT</a>
                </div>
            </div>
            {% include('workOrder/_inProgressWorkOrder.html.twig') %}
            {# display navigation #}
                <div class="navigation">
                    {{ knp_pagination_render(workOrders) }}
                </div>
        </section>
        <aside class="col-2">
        </aside>
    </div>
{% endblock %}
