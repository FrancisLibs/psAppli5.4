{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <main class="row mt-5">
        <article class="col-10 mx-auto">
            <div class="row align-items-center">
                <div class="col-3">
                    <h3>Liste des Utilisateurs</h3>
                </div>
                <div class="col-2">
                    <a class="btn btn-primary btn-sm" href="{{ path('app_register') }}" role="button">Nouvel utilisateur</a>
                </div>
            </div>
            <div class="mt-5">
                {% include 'user/_userList.html.twig' with {users: users} only %}
            </div>
        </article>
    </main>

{% endblock %}
