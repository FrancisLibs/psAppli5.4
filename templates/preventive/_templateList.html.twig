<div class="row">
    {{ form_start(form, {attr: {class: 'filter js-filter-form'}}) }}
        <table class="table table-sm table-striped">
            <thead>
                {% include 'preventive/_filter.html.twig' %}
                {% include 'preventive/_sorting.html.twig' %}
            </thead>
            <tbody class="js-filter-content">
                {% include 'preventive/_templates.html.twig' %}
            </tbody>
        </table>
    {{ form_end(form) }}
</div>