<tr>
    <th>{{ knp_pagination_sortable(workorders, 'N°', 'w.id') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(workorders, 'Travaux', 'w.subject') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(workorders, 'Machines', 'w.machines') }}</th>
</tr>