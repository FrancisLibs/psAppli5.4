<tr class="sorting">
    <th>{{ knp_pagination_sortable(templates, 'N°', 't.templateNumber') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(templates, 'Travaux', 't.request') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(templates, 'Date', 't.nextDate') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(templates, 'Machines', 't.machines') }}</th>
</tr>