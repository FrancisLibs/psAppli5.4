<tr>
    <th>{{ knp_pagination_sortable(delivery_notes, 'N°', 'd.id') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(delivery_notes, 'Numéro', 'd.number') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(delivery_notes, 'Date', 'd.date') }}</th>
    <th class="text-center">{{ knp_pagination_sortable(delivery_notes, 'Fournisseur', 'd.provider.name') }}</th>
</tr>