<form method="post" action="{{ path('delivery_note_delete', {'id': delivery_note.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ delivery_note.id) }}">
    <button class="btn">Delete</button>
</form>
