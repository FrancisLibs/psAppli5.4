<form method="post" action="{{ path('delivery_note_delete', {'id': deliveryNote.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deliveryNote.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
