{{ form_start(form) }}  
    <div class="row justify-content-start align-items-end">
        <div class="col-3">
            {{ form_label(form.deliveryNoteNumber) }}
            {{ form_widget(form.deliveryNoteNumber) }}
        </div>
        <div class="col-3 offset-1">
            {{ form_label(form.date) }}  
            {{ form_widget(form.date) }}  
        </div>
    </div>
    <div class="row mt-5 mb-4">
        <div class="col-5 d-flex justify-content-between align-items-center">
            <h4 class="text-primary">Pièces détachées</h4>
            <a class="btn btn-sm btn-primary" href="{{ path('part_index', { 'mode': 'selectDeliveryNoteParts' }) }}">Selection</a>
        </div>
    </div>
    <div class="row">
        <div class="col-3 text-center">
            <button type="submit" class="btn btn-primary pull-right w-100 mt-3">Valider</button>
        </div>
    </div>
{{ form_end(form) }}