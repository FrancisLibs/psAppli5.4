<form method="post" action="{{ path('accountType_delete', {'id': accountType.id}) }}" onsubmit="return confirm('Veux-tu vraiment supprimer ce type de compte ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accountType.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
