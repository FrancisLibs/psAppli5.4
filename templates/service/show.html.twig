{% extends 'base.html.twig' %}

{% block title %}Service
{% endblock %}

{% block body %}
	<h1>Service</h1>
	<div>
		<p>Organisation :
			{{ service.organisation.designation }}</p>
		<p>Nom :
			{{ service.name }}</p>
	</div>
	<div class="d-flex justify-content-start align-items-center flex-wrap">
		<a class="btn btn-primary btn-sm me-3" href="{{ path('service_index') }}" role="button">back to list</a>
		<a class="btn btn-primary btn-sm me-3" href="{{ path('service_edit', {'id': service.id}) }}" role="button">edit</a>
		{{ include('service/_delete_form.html.twig') }}</div>

{% endblock %}
