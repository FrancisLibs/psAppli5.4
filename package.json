{
  "devDependencies": {
    "@symfony/stimulus-bridge": "^2.0.0",
    "@symfony/webpack-encore": "^5.2.0",
    "core-js": "^3.0.0",
    "regenerator-runtime": "^0.13.2",
    "stimulus": "^2.0.0",
    "webpack-notifier": "^1.6.0"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev-server": "encore dev-server",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider encore dev",
    "watch": "encore dev --watch",
    "build": "NODE_OPTIONS=--openssl-legacy-provider encore production"
  },
  "dependencies": {
    "chart.js": "^4.3.0"
  }
}
